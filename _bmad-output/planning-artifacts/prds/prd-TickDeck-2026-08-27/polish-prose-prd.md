本文档用于帮助产品、UX、架构、开发、QA、安全和开源维护者，在不把待证 thesis 当成事实的前提下，准确理解 TickDeck v1.0 的条件式产品承诺、分阶段 Gate 和可验收要求。原文采用正式、审慎、规范性强的产品语言，并有意保留 Agent、Gate、Parity Rubric、fixture 等领域术语；以下建议保留这种语气，只处理会妨碍理解的语法、指代、术语和验收语义问题。配置要求的 `project-context.md` 在仓库中未找到，因此本轮没有额外项目上下文可加载。

| Pass | Original Text | Revised Text | Changes |
|---|---|---|---|
| prose | §4 UJ-1 — “若数据不足，Agent 明确降级、拒绝或返回零候选，不补猜结果。” | “若数据不足，Agent 明确降级、拒绝或返回零候选，不猜测或补全结果。” | “补猜”不是稳定搭配，可能让读者不确定禁止的是猜测、补全还是二者；改成并列动词，不改变拒绝编造数据的要求。 |
| prose | §4 UJ-2 — “Agent 形成市场、周期、信号、仓位、风控、基准和执行假设清晰的策略契约……” | “Agent 形成一份明确市场、周期、信号、仓位、风控、基准和执行假设的策略契约……” | 修复多层定语的依附关系；明确列举项是策略契约需要说明的内容。 |
| prose | §6.4、§13 A-05 — “若合同只允许更低粒度，不能通过 v1.0 parity 门”；“任务级 parity” | §6.4：“若合同仅允许比上述要求更粗的时间粒度，则不能通过 v1.0 Parity Rubric 验收。”<br>§13 A-05：“任务级 Parity Rubric” | “更低粒度”可能被理解为更细或更粗；明确为更粗的时间粒度。并统一使用词汇表中的专名 `Parity Rubric`，避免读者误以为这里指另一套验收。 |
| prose | §7.3 FR-016 — “用户可以用行情、技术、基本面、市场和连接器支持的字段组合条件、排序和排除项。” | “用户可以使用行情、技术、基本面、市场和连接器支持的字段，组合筛选条件、排序规则和排除项。” | 增加必要停顿并补全并列名词，使“字段”与“组合”的语法关系清楚。 |
| prose | §7.3 FR-017 — “在后续数据时间上复跑并比较结果变化。” | “基于后续时点的数据复跑，并比较结果变化。” | “在……时间上”不自然且可能被误读为修改时间字段；明确后续时点的数据是复跑输入。 |
| prose | §7.6 FR-033 — “每次运行必须执行 CPU、内存、墙钟、输出、数据请求和并发配额；只允许锁定版本的内置 API 和允许依赖。” | “每次运行必须强制执行 CPU、内存、实际经过时间、输出、数据请求和并发配额；只允许使用版本锁定的内置 API 和明确列入允许范围的依赖。” | “执行配额”和“允许依赖”缺少准确的动宾关系；补全后更容易转成实现与验收条件。“实际经过时间”也为后文的墙钟时间提供直观含义。 |
| prose | §7.8 FR-046 — “用户可以提交支持的市价、限价、撤单和数量变更……” | “用户可以提交受支持的市价单或限价单，也可以撤单或变更订单数量……” | 原句把订单类型与订单操作并列，可能被解析成四种订单类型；拆开后保持原有能力范围。 |
| prose | §7.10 FR-066 — “系统必须按模型精确版本、提示版本和工具集验证连接、流式输出、结构化输出、工具调用、上下文、取消、多步语义和提示注入抵抗……” | “系统必须针对精确的模型版本、提示版本和工具集组合，验证连接、流式输出、结构化输出、工具调用、上下文长度、取消能力、多步语义正确性和提示注入抵抗能力……” | 明确资格绑定的是三者组合；补全“上下文”“取消”“多步语义”的被测属性，避免不同实现选择不同测试含义。 |
| prose | §7.12 FR-082 — “v1.0 后公共扩展契约遵循语义化版本，优先兼容性增加……” | “v1.0 后，公共扩展契约遵循语义化版本，优先采用向后兼容的增量变更……” | “优先兼容性增加”不是明确的变更策略；修订后保持原有兼容性承诺，并说明指的是增量变更。 |
| prose | §7.13 FR-088、FR-092、FR-093、FR-096、FR-097 首次使用 — “SSRF”“DNS rebinding”“nonce”“事务 outbox”“追加式 schema”“secret reference”“SBOM” | 首次出现时分别写为“服务器端请求伪造（SSRF）”“DNS 重绑定（DNS rebinding）”“一次性随机数（nonce）”“事务发件箱（transactional outbox）”“追加式记录模式（append-only schema）”“秘密引用（secret reference）”“软件物料清单（SBOM）”；后文继续使用简称。 | 文档受众包含产品、UX 和贡献者，不全是安全或后端专家。按首次出现释义，保留技术术语，同时减少跨职能读者误解；各术语的约束不变。 |
| prose | §8.3 — “默认资源档案：编译墙钟 10 秒；指标预览墙钟 5 秒、内存 512 MiB、输出 10 MiB；单次策略回测墙钟 60 秒、内存 1 GiB、输出 50 MiB。” | “默认资源档案：编译的最长实际经过时间为 10 秒；指标预览的最长实际经过时间为 5 秒，内存上限为 512 MiB，输出上限为 10 MiB；单次策略回测的最长实际经过时间为 60 秒，内存上限为 1 GiB，输出上限为 50 MiB。” | 原句省略了“最长”和“上限”，且“墙钟”对非工程读者不直观；补全单位对应的限制类型，不改变任何数值。 |
| prose | §8.6 — “当前稳定次版本及前一个次版本在后者发布后 90 天内获得严重安全修复……” | Consider: “当前稳定次版本发布后 90 天内，前一个次版本也获得严重安全修复……”？ | “后者”按语法指“前一个次版本”，会让 90 天窗口的起算点不明确。该句需要作者确认究竟从当前稳定次版本还是前一个次版本的发布日期起算；示例按常见支持窗口表达，但不应在未确认时直接应用。 |
| prose | §9.3 NFR-018 — “发布版本不得包含已知严重级别依赖漏洞……” | “发布版本不得包含已知的严重级依赖项漏洞……” | 明确“严重级”修饰漏洞、“依赖项”说明漏洞来源，避免把“严重级别依赖”读成一种依赖类型。 |
| prose | §9.5 NFR-033、NFR-034 — “错误 Host/Origin、CSRF、跨站 WebSocket 和 DNS rebinding 测试成功率为 0”；“出站测试必须覆盖……成功率为 0” | NFR-033：“针对未授权状态修改、错误 Host 或 Origin、CSRF、跨站 WebSocket 和 DNS 重绑定的攻击尝试，成功率必须为 0；远端模式下，伪造转发头和绕过上游端口的尝试成功率也必须为 0。”<br>NFR-034：“出站测试必须覆盖私网与云元数据 SSRF、DNS 重绑定、危险重定向和跨主机凭据转发；上述攻击尝试的成功率必须为 0。” | “测试成功率为 0”字面上表示测试套件不能通过，与安全目标相反；明确为攻击或绕过尝试的成功率为 0。所有攻击面和阈值保持不变。 |
| prose | §10.2 SM-07 — “固定合规套件中的已知逃逸、越权和宿主影响成功为 0；资源档案的强制终止成功率为 100%。” | “固定合规套件中，已知逃逸、越权以及影响宿主的测试用例成功数均为 0；对超出资源档案限制的运行，强制终止成功率为 100%。” | “宿主影响成功”缺少清楚的主谓关系；同时补出强制终止针对的对象，避免把正常运行也理解为必须终止。两个阈值不变。 |
| prose | §11 风险表 — “默认拒绝 DataUsePolicy”；“EgressPolicy、最小化、目的允许列表、凭据隔离” | “默认拒绝的数据使用策略”；“出站策略、最小化、目的地址允许列表、凭据隔离” | 正文和词汇表已经使用“数据使用策略”“出站策略”；风险表突然改用未定义的英文名，可能被误认为不同组件。统一既有术语，不改变控制机制。 |

总结：共 **16 条 prose 建议**。主要影响是消除 3 处可能反向解读的安全/支持窗口表述、修复 7 处语法或并列关系，并统一首次出现的安全术语与既有产品术语。当前文档精确总量为 **17,278 words**；本轮不以压缩为目标，全部接受后预计净增约 **10–30 words（低于 0.2%）**，没有长度目标需要校验。所有建议均保留原有内容、阈值、FR/NFR/SM/UJ/OQ/A 编号、安全默认值、Go/Stop 条件和假设状态；§8.6 的支持窗口起算点仍需作者确认后才能应用。
