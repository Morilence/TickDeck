这份文档用于帮助 PRD 下游的架构、UX、安全、供应商验证和开源维护者，在不把调研线索误当成已验证事实的前提下，准确理解技术约束、证据边界、待决问题与发布门槛。

| Pass | Original Text | Revised Text | Changes |
|---|---|---|---|
| prose | §产品验证状态 — “因此 S0-V 必须使用同一真实任务做 Agent-on 与现有工具链对照”以及“SM-00 未通过即停止平台化建设。” | “因此，PRD 中定义的 S0-V 必须使用同一真实任务，对 Agent-on 与现有工具链进行对照评估”以及“若 PRD 中定义的 SM-00 未通过，则停止平台化建设。” | 明确 S0-V、SM-00 的定义位于 PRD，补足条件关系；不改阶段门槛。 |
| prose | §产品验证状态 — “通过后，双市场真实数据、完整安全与恢复门仍是 v1.0 不可豁免条件。” | “S0-V 通过后，双市场真实数据、完整的安全与恢复验收门槛仍是 v1.0 的硬性发布条件。” | 补出“通过”的主语，并将含混的“恢复门”改为全文实际表达的“恢复验收门槛”。 |
| prose | §已给定的技术与交付约束、§交付与运行边界 — “产品不建设完整用户体系；远端访问认证交由反向代理承担。”以及“远端认证、HTTPS、访问日志和外层限流由部署者配置的反向代理承担。” | “产品不建设完整用户体系；远端入口的身份认证交由反向代理承担。”以及“远端入口的身份认证、HTTPS、访问日志和外层限流由部署者配置的反向代理承担。” | 将“访问认证/远端认证”统一为“远端入口的身份认证”，避免与后文应用仍需实例级受保护会话的要求产生范围误读。 |
| prose | §市场数据厂商接入现实 → 证据边界 — “‘用户持有账号’只解决身份验证的一部分，不自动授予缓存、展示、再输出、发送给外部模型或公网再分发权。” | “‘用户持有账号’只解决访问身份验证的一部分，并不自动获得缓存、展示、再输出、发送给外部模型或公网再分发的授权。” | 区分身份验证与数据使用授权，消除“授予……权”的生硬搭配；不改变许可边界。 |
| prose | §市场数据厂商接入现实 → 厂商表 Choice 行 — “是否存在适合 Fastify 的正式 HTTP 接口” | “是否存在适合服务端集成的正式 HTTP 接口” | Fastify 是既定实现约束，不是判断厂商接口可集成性的协议标准；改为结果导向的表述，避免误解为厂商需专门适配 Fastify。 |
| prose | §市场数据厂商接入现实 → 厂商表 iFinD 行 — “最可能优先获得试用验证，但当前仍未联调” | “三者中最可能率先获得试用验证，但当前仍未联调” | 补足比较范围，删除“最可能优先”的重复语义；保留未联调的证据边界。 |
| prose | §竞品与邻近方案证据 — “综合调研尚未发现一个经官方证据验证、同时满足……的现成产品。” | “截至调研截止日，现有官方资料尚未证明存在同时满足……的现成产品。” | 避免把“产品”写成被官方证据验证的对象，改为准确描述资料所能支持的结论；保留省略号所代表的原有条件清单。 |
| prose | §Agent 执行与审批参考 — “授权载荷包含受保护会话或可信代理主体、参数哈希、状态版本、nonce、有效期和使用次数。” | “授权载荷包含主体信息（来自受保护会话或可信代理）、参数哈希、状态版本、nonce、有效期和使用次数。” | 消除“受保护会话或可信代理主体”在并列层级上的歧义，明确两者是主体信息的来源。 |
| prose | §Agent 执行与审批参考 — “业务副作用与追加式审计通过 transaction outbox 或等价机制保持可恢复一致。” | “通过 transaction outbox 或等价机制，确保业务副作用与追加式审计在故障恢复后保持一致。” | 改正生硬的修饰关系，明确“一致”发生在故障恢复后；技术机制与约束不变。 |
| prose | §TypeScript 沙箱证据与方案边界 — “SES/Compartment 可提供对象能力隔离，但仍需处理无限循环、内存消耗和宿主注入能力，因此不能单独替代粗粒度隔离。” | “SES/Compartment 可提供对象能力隔离，但仍需处理无限循环、内存消耗和宿主注入的能力，因此不能单独替代粗粒度隔离。” | 补足“宿主注入的能力”的所属关系，避免将“宿主注入能力”误读为一种隔离能力。 |
| prose | §TypeScript 沙箱证据与方案边界 — “S0 前需要决定支持平台和粗粒度隔离；每个发布构建在对应平台运行固定沙箱合规套件。” | “S0 前需要确定受支持的平台和粗粒度隔离方案；每个发布构建都必须在对应平台上运行固定的沙箱合规测试套件。” | 补齐“决定”的宾语和第二句的义务语气，明确套件用于测试；不改变验证门。 |
| prose | §模型接入边界 — “模型配置档案至少需要提供商、Base URL、API Key、Model ID、自定义请求头、超时、重试、上下文、价格与默认参数。” | “模型配置档案至少需要包含提供商、Base URL、API Key、Model ID、自定义请求头、超时、重试、上下文长度、计价信息与默认参数。” | 补足谓语，并把易与证券价格混淆的“价格”明确为模型“计价信息”；与既定模型配置边界一致。 |
| prose | §模型接入边界 — “连接、对话或结构化工具握手只允许无副作用的单步 R0 查询” | “连接测试、对话测试或结构化工具握手测试只允许执行无副作用的单步 R0 查询” | 明确这些词指测试动作，而非所有连接或对话，避免把能力测试限制误读为运行期产品限制。 |
| prose | §扩展与公共接口取舍 — “语义化版本、兼容性增加优先和明确废弃期。” | “遵循语义化版本，优先采用向后兼容的增量变更，并设置明确的废弃期。” | 将难以解析的名词串改为三个并列动作；兼容政策不变。 |
| prose | §可执行数据与出站策略边界 — “策略需要继承到派生物、索引和备份，并维护数据资产清单，以便到期清理和不可复现标记。” | “用途与出站约束必须继承至派生物、索引和备份；系统还需维护数据资产清单，以便在授权到期时完成清理和不可复现标记。” | 明确需要继承的是“约束”而不是抽象的“策略”，并补出执行主体和触发时点。 |
| prose | §应用层访问边界 — “代理身份仅用于审计，不升级为 v1.0 用户/RBAC。” | “可信代理提供的身份信息仅用于审计；v1.0 不据此引入用户体系或 RBAC。” | RBAC 不是可被“升级为”的身份对象；修改后清楚区分审计身份信息与产品权限模型。 |
| prose | §秘密、备份与数据生命周期 — “所有凭据以 secret reference 进入普通配置，支持创建、轮换、撤销和失效诊断。” | “普通配置只保存 secret reference，不保存凭据值；秘密管理机制需支持创建、轮换、撤销和失效诊断。” | 直接说明普通配置与凭据值的边界，并补出支持生命周期操作的主体；秘密要求不变。 |

简短总结：共 17 处 prose 修改，按理解影响排序。修改仅消除范围歧义、术语误读和句法负担，保留正式审慎的技术语气、全部证据边界、链接、安全约束与技术决策。原文共 5,087 词；这些修改不以压缩为目标，预计净字数变化不足 30 词（低于 0.6%），也不引入任何结构调整。
