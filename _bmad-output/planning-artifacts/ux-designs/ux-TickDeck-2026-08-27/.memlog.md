---
topic: TickDeck/UX
updated: 2026-08-27T21:01
---

- (decision) 工作模式：Create + Coaching path；由 UX facilitator 分析并提出推荐方案，用户逐项拍板。
- (decision) 唯一已确认上游输入：prd-TickDeck-2026-08-27/prd.md 与 addendum.md；严格继承其产品边界、阶段门、稳定 FR/NFR 和开放项，不擅自扩大范围。
- (decision) Finalize 时运行全部 Reviewer Gate，并在发现项解决后完成结构与文案润色。
- (event) 已分别提取并核对 prd.md 与 addendum.md；后续 UX 决策必须保留 FR-001 至 FR-100、NFR-001 至 NFR-040 的稳定编号和原名。
- (decision) 继承的 Foundation：v1.0 为桌面优先 B/S Web；React + shadcn/ui（Base UI）+ Tailwind CSS；本地与远端自托管同能力；无原生桌面壳和移动端验收承诺。
- (decision) 继承的工作区边界：一实例一受信工作区；无用户、组织、RBAC、多租户或逐用户归属；远端认证由反向代理承担，但本地和远端均需实例级受保护会话。
- (decision) 阶段表达必须区分 S0-V 至 S5：S0-V 仅一条合法真实数据路径、只读筛选、受限 R0 Agent；完整 v1.0 的八组能力不可被提前暗示为已获实施许可。
- (event) Concern scan 已开启：可访问性、桌面平台与键盘、双语、数据真实性和许可、Agent 风险/审批/成本、长任务、通知、显式降级、共享工作区边界、外部故障与非离线承诺；品牌、暗色模式、动效与密度仍待 Discovery。
- (decision) 新增视觉继承源：/usr/local/src/CoJynt/apps/web/components.json。TickDeck 继承其 shadcn style=base-vega、Base UI 实现语汇、Tailwind CSS variables、neutral base、Lucide 图标、menuColor=default 与 menuAccent=subtle；不继承 CoJynt 的产品 IA、功能或业务文案。
- (event) 已实时核验 CoJynt 关联样式：global.css 提供 neutral 的亮/暗语义 token 与 radius=0.625rem；components.json 本身不决定 TickDeck 是否暴露主题切换，暗色模式仍为待决 UX 项。
- (event) Discovery 的唯一 anything-else probe 已关闭：用户补充 CoJynt components.json 为必须继承的风格输入。
- (decision) 规格分层采用方案 A：DESIGN.md 为统一视觉系统；EXPERIENCE.md 描述完整目标 v1.0，并为每个界面、流程、组件标注最早进入的 S0-V 至 S5 阶段。0.x 产品只呈现已通过 Gate 的能力，不用禁用菜单或占位入口暗示后续能力已承诺。
- (decision) 主工作结构采用方案 A：图表工作台为核心，Agent 作为伴随式侧栏共享当前标的、筛选器、脚本、回测或组合上下文；复杂产物可进入独立全页。Agent 不是独立聊天首页，专业工具也不降格为聊天结果附件。
- (decision) 应用壳层采用方案 A：研究工作与系统管理位于同一壳层、在导航中分区；该分区不形成角色、身份或权限差异。数据源、模型、健康和备份保持直接可达，但不与研究主任务争夺视觉重心。
- (decision) 一级导航采用按任务域组织：研究区包含工作台、筛选、策略实验室、提醒、模拟组合；系统区包含连接与模型、运行与健康、扩展。自选、基本面、资讯、绘图和指标位于工作台上下文；Agent 不占一级导航，通过伴随侧栏和运行历史进入。具体双语标签后续定稿。
- (decision) Agent 上下文绑定采用方案 A：面板自动建议当前产品上下文并以可检查、可移除的上下文项显示；运行启动时冻结上下文快照。运行期间页面切换不改变输入，新增或更换上下文必须显式重新绑定，并进入运行清单。
- (decision) 风险确认采用运行时间线内渐进式 Gate：R0 自动执行并留痕；R1 首次就地确认授权范围、对象、有效期且可撤销；R2 暂停运行并展开专用确认面板，显示对象、参数、成本、出站范围、组合影响、有效期和可撤销性；R3 明确阻止且无覆盖入口。待确认运行可从运行历史恢复。
- (decision) 数据溯源采用方案 A：每个数据驱动界面常驻最小信任条，显示来源、数据时间、新鲜度及实时/延时/演示/部分状态；一步展开时区、币种、复权、完整性、连接器版本与许可用途。缺失、陈旧、超额和不支持必须贴近受影响内容呈现，不得只用 Toast。
- (event) 已生成工作台视觉方向对比：.working/directions-workbench-density.html（A 密集指挥台、B 平衡分析台、C 证据优先画布），另有同名 PNG 预览。三版功能和 neutral 色系一致，仅比较密度、层级、留白与视觉重心；尚未选定。
- (change) 用户拒绝第一版工作台视觉方向对比：三版差异不足，主要只改变间距、字号和侧栏宽度，无法形成可辨识的视觉人格。该版保留为 .working 探索记录但不得晋升为正式视觉参考；需重新渲染结构、明暗、字体和证据语言均明显不同的方向。
- (event) 已生成第二版三套明显差异化视觉方向：direction-terminal-dark.html（深色高密专业终端）、direction-calm-canvas.html（浅色专注分析画布）、direction-audit-ledger.html（审计式证据台），各有 PNG 预览。三者保持已确认 IA；当前尚未选定。
- (decision) 视觉方向选择：A，深色高密专业终端。正式视觉语言以 neutral/OKLCH 深色底、单色层级、紧凑密度、等宽数字、图表主画布与右侧 Agent 风险面板为主；继承 CoJynt components.json 的 base-vega/Base UI/Tailwind/radius 设定，但不继承其产品 IA。
- (decision) 参考体系原则：工具侧交互借鉴 TradingView 等成熟交易研究工具；Agent 侧交互参考 Codex、Claude、Kimi。只继承已验证的交互机制，不复制品牌视觉，不引入 PRD 外的实盘交易、社区、付费增长或多 Agent 范围。待用户拍板具体借鉴映射。
- (decision) 参考体系映射已批准：A 分层继承。工具工作台采用 TradingView 式图表空间锚点、紧凑工具带、上下文面板、快速命令与布局恢复；Agent 采用 Codex 的任务/审阅生命周期、Claude 的计划与权限纪律、Kimi 的后台任务与成果交付；叠加 TickDeck 数据溯源、R0-R3、阶段门和事实边界。禁止像素复刻及范围扩张。
- (decision) Workbench 停靠方式采用 A 双侧伴随布局：全局导航位于最左；左侧可折叠、可调宽的上下文抽屉承载自选、对象树和数据窗口；中央为不可被挤失的图表主画布；右侧可折叠、可调宽的 Agent 面板。面板尺寸与折叠状态按工作区记忆；空间不足时优先收起左侧，运行中的 Agent 和 R2 确认不得被遮挡。
- (decision) Agent 运行过程采用 A 分层时间线：节点类型固定为计划、当前步骤、工具事件、Gate、结果；常规工具调用默认折叠，高风险、失败、重试、数据降级与待确认自动展开；原始日志进入详情，不展示模型思维链。最终结果必须区分事实、确定性计算、模型解释、未知项，并附运行清单。
- (decision) Agent 结果落位采用 A：侧栏保留摘要、风险、未知项和下一步；完整筛选报告、回测结果、组合分析等结构化产物在中央审阅页打开，提供证据、参数、计算、图表标注和运行清单。关闭产物恢复进入前的图表、标的、时间范围、面板和滚动状态；不得把复杂结果塞入窄侧栏或强制脱离工作台。
- (override) 主题边界由用户明确覆盖推荐：v1 必须同时支持深色、浅色、跟随系统三种模式。DESIGN.md 需给出两套完整语义 token；EXPERIENCE.md 需定义三态切换、持久化、系统变化响应，并对图表、A/HK 行情色、风险态、焦点态和 WCAG 2.2 AA 在两套主题下分别验收。
- (decision) 主题默认与持久化采用 A：首次访问默认跟随系统；用户显式选择深色或浅色后覆盖系统，重新选择跟随系统才恢复实时响应。主题偏好仅保存在当前浏览器/设备，不写入共享工作区或服务端；系统主题变化不得中断任务、重置图表或触发强烈过渡动画。
- (decision) 行情涨跌色采用 A：A/HK 全产品统一红涨绿跌，深浅主题语义一致。涨跌信息必须同时提供正负号、方向箭头、标签或图形形态，不得仅靠颜色；系统成功、失败、警告、风险级别和数据质量使用独立语义 token，禁止直接复用行情涨跌色。
- (decision) 中英文界面采用 A：首次访问依据浏览器语言选择简体中文或 English，之后仅在当前浏览器保存显式选择，不写入共享工作区。一次会话中的导航、控件和系统状态保持单一界面语言；证券简称、公告、研报、新闻等来源内容保留原文并标注语言；Agent 默认跟随界面语言，任务可显式指定输出语言。
- (decision) 产品内通知采用 A：持久通知中心是提醒触发、Agent 长任务完成、待确认、失败和系统健康事件的权威入口，支持已读/未读、类型筛选与深链到运行、提醒或证据；Toast 仅用于当前页面的即时提示，消失不得造成信息丢失。Webhook 仅在部署者显式配置、策略允许并完成相应确认后发送，状态、重试和失败原因可诊断；浏览器系统通知不属于 v1 必需范围。
- (decision) 键盘操作采用 A 分层快捷键：Ctrl/Cmd+K 为全局命令面板；图表、Agent 输入、表格和脚本编辑器各自定义焦点作用域，冲突时以当前焦点组件为准；菜单和工具提示必须可发现对应快捷键。删除、覆盖、外发数据、启用策略、模拟订单及其他 R2 操作不得由单键或快捷键直接完成，仍需完整确认界面。
- (decision) 桌面布局验收采用 A：最低视口 1280×720，最佳体验为 1440×900 及以上；宽度至少 1600 时允许左右伴随面板同时展开，1280–1599 时左侧上下文默认收起且 Agent 保持可见，低于 1280 时进入单面板专注布局并提示空间受限但不纳入 v1 验收。任何宽度下 R2 确认、错误和数据状态不得被裁切或遮挡；不形成移动端承诺。
- (decision) Agent 长任务生命周期采用 A：切换页面、刷新或关闭浏览器不等于取消，运行从运行历史恢复查看；仅显式停止才请求终止。状态统一为排队、运行中、待确认、已暂停、已完成、失败、已取消、已中断；服务重启后明确显示恢复或中断结果。待 R2 确认到期后安全失效，绝不自动执行。主要 Discovery 至此关闭，进入 Finalize。
- (event) Finalize 初稿已生成：DESIGN.md 与 EXPERIENCE.md 已按已批准决策落盘；关键视觉已覆盖 S0-V 筛选证据审阅、目标 v1.0 深/浅主题工作台 + Agent/R2、S3 策略实验室/回测、S1 连接与健康诊断。调和报告已对 PRD 与 addendum 复核，开放发现将在 Reviewer Gate 前后修正。
- (decision) Mock coverage 选择 A：现有视觉覆盖 S0-V 筛选证据、目标 v1 深/浅工作台 + Agent/R2、S3 策略/回测、S1 连接与健康，足够作为关键视觉参考；提醒管理、模拟组合管理、通知中心、扩展管理明确为 spine-only，按规格中的表格、状态、审阅画布和诊断模式实现，不再补独立 mock。
- (override) 实现视觉基线进一步明确：最终效果以 /usr/local/src/CoJynt/apps/web/components.json 的 shadcn style=base-vega、Base UI 默认 variant，以及 /usr/local/src/CoJynt/apps/web/src/styles/global.css 提供的默认 Tailwind/shadcn 语义 token 预设为准。mock 仅约束布局、密度、信息层级与状态关系；基础背景、前景、卡片、边框、ring、sidebar、chart 等不得用 mock 手写值覆盖。TickDeck 只新增 shadcn 缺失的行情、数据质量和 R0-R3 语义 token。
- (event) Finalize 完成：rubric 八类均为 strong；Accessibility 与 Financial Safety 的 Critical/High/Medium/Low 均为 0。结构与文案建议已应用，reconciliation 已刷新，DESIGN.md 与 EXPERIENCE.md 晋级为 final；该状态只表示 UX 文档合同定稿，不替代实现、部署、真实连接、WCAG 或发布 Gate 证据。
- (override) 最终正式 UX 工件不出现或依赖 CoJynt；此前参考配置已内化为 TickDeck 自身的 shadcn 官方 base-vega + Base UI 基线。正式文件移除跨仓路径、commit、hash 与品牌引用；交互基础组件以 shadcn 官方 registry 为准。
- (event) spines finalized：正式 UX 工件已移除跨仓品牌与来源依赖；TickDeck 自包含采用 shadcn 官方 base-vega + Base UI 组件和 Tailwind semantic token 基线；定向 Reviewer Gate 8/8 strong，finding 0/0/0/0。
